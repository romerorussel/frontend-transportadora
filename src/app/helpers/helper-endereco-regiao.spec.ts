import { HelperEnderecoRegiao } from './helper-endereco-regiao';

describe('HelperEnderecoRegiao', () => {
  it('should create an instance', () => {
    expect(new HelperEnderecoRegiao()).toBeTruthy();
  });
});
